Sources for the plugin `plugin.materialcolors`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.materialcolors" = {
            publisherId = "io.joehinkle",
        },
    },
}
```
